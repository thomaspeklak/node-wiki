extends layout

block content
    h1.title.edit(contenteditable)= page.title

    div.tags
        h2.label.label-success Tags
        div.edit(contenteditable)= page.tags.join(", ")

    .content.editable
        != page.content
