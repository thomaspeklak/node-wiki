extends layout

block content
    div.navbar
        div.navbar-inner
            a(href="#").brand All pages
            ul.nav
                li
                    a(href="/pages") List
                li.active
                    a(href="/pages/covers") Cover
    ul
        each page in pages
            li.preview
                a(href=page.path)
                    img(src="#{page.image}").img-polaroid
                    p= page.title



